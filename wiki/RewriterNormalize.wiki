#summary R_normalize(F)
R_normalize(F) - ([http://code.google.com/p/aic-expresso/source/browse/trunk/src/main/java/com/sri/ai/grinder/library/equality/cardinality/direct/core/Normalize.java implementation])
<pre>
inputs: F is an expression.
return: a normalization of E, that is, an equivalent expression with all if-then-elses externalized.
        The expression is also simplified per R_simplify.
</pre>
